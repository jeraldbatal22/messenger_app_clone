import styled from "styled-components"

const Searchbar = () => {
  return (
    <HeaderSearch>
      <input /><Search className="search_icon" />
    </HeaderSearch>
  )
}

export default Searchbar

const HeaderSearch = styled.div`

`